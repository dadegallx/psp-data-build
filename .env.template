# dbt Database Connection Configuration
# ======================================
# Copy this file to .env and fill in your actual values
#
# The dbt project reads source data from the specified database
# and builds staging (views) + marts (tables) in the same database.
#
# Source schemas: ps_network, ps_families, data_collect
# Target schemas: staging (views), marts (tables)

export DBT_HOST="your-postgres-host"
export DBT_USER="your-username"
export DBT_PASSWORD="your-password"
export DBT_PORT="5432"
export DBT_DBNAME="your-database"
export DBT_SCHEMA="analytics"  # Default schema prefix for dbt models
